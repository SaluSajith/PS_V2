<?xml version="1.0" encoding="utf-8"?>
<resources>

    <declare-styleable name="CircleImageView">
        <attr name="border_width" format="dimension" />
        <attr name="border_color" format="color" />
    </declare-styleable>

    <declare-styleable name="CustomStyle">
        <attr name="pret_typeface" format="string" />
        <attr name="rating_count" format="integer" />
        <attr name="rating_size" format="integer" />
        <attr name="rating_progress" format="integer" />
        <attr name="rating_selectable" format="boolean" />
        <attr name="rating_selected_drawable" format="integer" />
        <attr name="rating_unselected_drawable" format="integer" />
    </declare-styleable>

    <declare-styleable name="NotificationBadge">
        <attr name="nbTextSize" format="dimension" />
        <attr name="nbTextColor" format="color" />
        <attr name="nbBackground" format="reference" />
        <attr name="nbAnimationEnabled" format="boolean" />
        <attr name="nbAnimationDuration" format="integer" />
        <attr name="nbMaxTextLength" format="integer" />
        <attr name="nbEllipsizeText" format="string" />
    </declare-styleable>

    <declare-styleable name="HiveProgressView">
        <attr name="hive_animDuration" format="integer" />
        <attr name="hive_maxAlpha" format="integer" />
        <attr name="hive_color" format="color" />
        <attr name="hive_rainbow" format="boolean" />
        <attr name="hive_cornerRadius" format="integer" />
        <attr name="hive_shrink" format="boolean" />
    </declare-styleable>

    <!-- Declare custom theme attributes that allow changing which styles are
         used for button bars depending on the API level.
         ?android:attr/buttonBarStyle is new as of API 11 so this is
         necessary to support previous API levels. -->
    <declare-styleable name="ButtonBarContainerTheme">
        <attr name="metaButtonBarStyle" format="reference" />
        <attr name="metaButtonBarButtonStyle" format="reference" />
    </declare-styleable>

</resources>